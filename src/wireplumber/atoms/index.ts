/** Writable atom that applies a Game/Voice crossfade through WirePlumber. */
export { setWirePlumberCrossfade } from "./crossfade.js";

/** Reactive state published by WirePlumber. */
export { wirePlumberState } from "./state.js";
